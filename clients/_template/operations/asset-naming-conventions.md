# Asset Naming Convention Template

Use predictable names so operators and scripts can locate assets fast.

Suggested pattern:

`<client-slug>_<show-slug>_<episode-or-date>_<asset-type>_<channel>.<ext>`

Example:

`client-show_ep-001_event-copy_linkedin.md`

# Validation Rules Template

- Every client must have a workbook manifest.
- Every episode file must include episode number, recording date, title, hosts, and published status.
- Workbook content rows should use approved dropdown values when they target workbook status fields.
- Sync scripts must support preview mode without external writes.

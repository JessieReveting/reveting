# Gmail Connector

Reserved for future Version 1 discovery snapshots.

Allowed responsibilities:

- Authenticate read-only when credentials are configured.
- Read relevant mailbox data.
- Normalize snapshots under `data/gmail/`.

No email sending, drafting, labeling, or automation belongs here until explicitly approved in a future automation layer.


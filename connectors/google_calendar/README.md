# Google Calendar Connector

Version 1 responsibilities:

- Authenticate with Google Calendar read-only credentials or normalize a manual JSON export.
- Read `ww@reveting.com` events with full event details.
- Normalize snapshots to `data/calendar/ww_reveting_events.json`.

No calendar updates, attendee changes, or automation belong here.

